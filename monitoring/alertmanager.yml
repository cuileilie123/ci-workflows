global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'service']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'push-bridge'

receivers:
  - name: 'push-bridge'
    webhook_configs:
      - url: 'http://webhook-bridge:8080/alert'
        send_resolved: true
